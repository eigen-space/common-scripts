const CommonScripts = require('./scripts/common/common');
const copy = require('./scripts/common/copy');
const parseProcessArgs = require('./scripts/common/parse-process-args');

module.exports = {
    CommonScripts,
    copy,
    parseProcessArgs
};